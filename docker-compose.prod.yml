services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fintech-app-prod
    env_file:
      - .env.prod
    ports:
      - "8080:8080"
    restart: unless-stopped

